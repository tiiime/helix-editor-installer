# Helix Editor Install Script

You can install pre-built binaries of helix if you are on macOS or Linux using the following simple command:

```shell
curl -L https://raw.githubusercontent.com/tiiime/helix-editor-installer/refs/heads/main/installer.sh | sh /dev/stdin
```
